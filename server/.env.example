# 山东省大中小学思政课一体化指导中心 - 环境变量配置示例
# 复制此文件为 .env 并根据实际情况修改配置

# ===========================================
# 基础配置
# ===========================================

# 应用运行环境 (development | production | test)
NODE_ENV=development

# 服务器端口
PORT=3000

# 应用版本（用于日志记录）
npm_package_version=1.0.0

# 服务器主机名（用于日志记录）
HOSTNAME=localhost

# ===========================================
# 日志配置
# ===========================================

# 日志级别 (debug | info | warn | error | fatal)
LOG_LEVEL=debug

# ===========================================
# 数据库配置
# ===========================================

# MongoDB 连接字符串
MONGODB_URI=mongodb://localhost:27017/sdszk

# MongoDB 连接选项
DB_MAX_RECONNECT_ATTEMPTS=5
DB_INITIAL_RETRY_DELAY=1000

# ===========================================
# Redis 配置
# ===========================================

# Redis 主机地址
REDIS_HOST=localhost

# Redis 端口
REDIS_PORT=6379

# Redis 密码（可选）
# REDIS_PASSWORD=your_redis_password

# Redis 数据库编号
REDIS_DB=0

# 开发环境是否启用 Redis（可选，默认为 false）
REDIS_ENABLED=false

# ===========================================
# JWT 认证配置
# ===========================================

# JWT 密钥（生产环境必须设置强密码）
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT 过期时间
JWT_EXPIRES_IN=1d

# ===========================================
# CORS 安全配置
# ===========================================

# 前端应用 URL（支持多个，用逗号分隔）
# 开发环境示例：
FRONTEND_URL=http://localhost:5173,http://localhost:3000

# 生产环境示例：
# FRONTEND_URL=https://yourdomain.com,https://www.yourdomain.com

# 生产环境额外域名（可选，用逗号分隔）
# PRODUCTION_DOMAINS=https://api.yourdomain.com,https://admin.yourdomain.com

# 开发环境额外端口（可选，用逗号分隔）
# DEV_PORTS=5174,5175,8080

# ===========================================
# 文件上传配置
# ===========================================

# 文件上传目录
UPLOAD_DIR=uploads

# 最大文件大小（字节）
MAX_FILE_SIZE=10485760

# 允许的文件类型
ALLOWED_FILE_TYPES=jpg,jpeg,png,gif,pdf,doc,docx,xls,xlsx,ppt,pptx

# ===========================================
# 邮件配置（可选）
# ===========================================

# SMTP 服务器配置
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-email-password

# ===========================================
# 安全配置
# ===========================================

# 密码加密轮数
BCRYPT_ROUNDS=12

# API 限流配置
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX_REQUESTS=100

# Session 配置
SESSION_SECRET=your-session-secret-change-this-in-production

# ===========================================
# 第三方服务配置（可选）
# ===========================================

# 阿里云 OSS 配置
# ALIYUN_OSS_REGION=oss-cn-hangzhou
# ALIYUN_OSS_ACCESS_KEY_ID=your-access-key-id
# ALIYUN_OSS_ACCESS_KEY_SECRET=your-access-key-secret
# ALIYUN_OSS_BUCKET=your-bucket-name

# 微信小程序配置
# WECHAT_APP_ID=your-wechat-app-id
# WECHAT_APP_SECRET=your-wechat-app-secret

# ===========================================
# 生产环境专用配置
# ===========================================

# 生产环境数据库（如果与开发环境不同）
# PROD_MONGODB_URI=mongodb://user:password@prod-server:27017/sdszk

# 生产环境 Redis（如果与开发环境不同）
# PROD_REDIS_HOST=prod-redis-server
# PROD_REDIS_PORT=6379
# PROD_REDIS_PASSWORD=prod-redis-password

# SSL 证书路径（生产环境 HTTPS）
# SSL_CERT_PATH=/path/to/certificate.crt
# SSL_KEY_PATH=/path/to/private.key

# ===========================================
# 监控和分析配置（可选）
# ===========================================

# Google Analytics
# GA_TRACKING_ID=UA-XXXXXXXXX-X

# Sentry 错误监控
# SENTRY_DSN=https://your-sentry-dsn

# ===========================================
# 开发工具配置
# ===========================================

# 是否启用 API 文档（Swagger）
ENABLE_API_DOCS=true

# 是否启用调试模式
DEBUG_MODE=true

# 是否启用性能监控
PERFORMANCE_MONITORING=false

# ===========================================
# 缓存配置
# ===========================================

# 缓存 TTL 配置（秒）
CACHE_TTL_SHORT=300
CACHE_TTL_MEDIUM=1800
CACHE_TTL_LONG=3600
CACHE_TTL_VERY_LONG=86400

# ===========================================
# 备份配置（可选）
# ===========================================

# 数据库备份目录
# BACKUP_DIR=/var/backups/sdszk

# 备份保留天数
# BACKUP_RETENTION_DAYS=30

# 自动备份间隔（小时）
# AUTO_BACKUP_INTERVAL=24
