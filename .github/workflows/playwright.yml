name: Playwright Tests
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

# 环境变量
env:
  NODE_VERSION: "20"
  CACHE_VERSION: v2

jobs:
  test:
    name: E2E Tests
    timeout-minutes: 60
    runs-on: ubuntu-latest

    # 添加服务容器
    services:
      mongodb:
        image: mongo:7.0
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --eval 'db.runCommand({ping: 1})'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7.2-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout代码
        uses: actions/checkout@v4

      - name: 设置Node.js环境
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      # 添加npm依赖缓存
      - name: 缓存前端依赖项
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ env.CACHE_VERSION }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-${{ env.CACHE_VERSION }}-
            ${{ runner.os }}-node-

      # 缓存Playwright浏览器
      - name: 缓存Playwright浏览器
        uses: actions/cache@v4
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ env.CACHE_VERSION }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-${{ env.CACHE_VERSION }}-
            ${{ runner.os }}-playwright-

      - name: 安装前端依赖项
        run: npm ci

      - name: 安装后端依赖项
        run: |
          cd server
          npm ci
          cd ..

      - name: 安装Playwright浏览器
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps chromium

      # 仅在缓存命中时安装系统依赖
      - name: 安装系统依赖
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: npx playwright install-deps chromium

      - name: 设置CI环境配置
        run: |
          # 复制CI环境配置文件
          cp .env.ci .env
          cp server/.env.ci server/.env

          # 设置脚本权限
          chmod +x scripts/ci-start.sh scripts/ci-stop.sh

      - name: 等待服务就绪
        run: |
          echo "等待MongoDB服务..."
          timeout 60 bash -c 'until mongosh --host localhost:27017 --eval "db.runCommand({ping: 1})" >/dev/null 2>&1; do sleep 2; done'
          echo "✅ MongoDB 已就绪"

          echo "等待Redis服务..."
          timeout 60 bash -c 'until redis-cli -h localhost -p 6379 ping >/dev/null 2>&1; do sleep 2; done'
          echo "✅ Redis 已就绪"

      # 启动应用服务
      - name: 启动后端服务
        run: |
          cd server
          npm start &
          echo $! > /tmp/backend.pid
          cd ..

          echo "等待后端服务启动..."
          timeout 120 bash -c 'until curl -f http://localhost:3000 >/dev/null 2>&1 || curl -f http://localhost:3000/api/health >/dev/null 2>&1 || curl -f http://localhost:3000/api/status >/dev/null 2>&1; do sleep 3; done'
          echo "✅ 后端服务已启动"

      - name: 启动前端服务
        run: |
          npm run dev &
          echo $! > /tmp/frontend.pid

          echo "等待前端服务启动..."
          timeout 120 bash -c 'until curl -f http://localhost:5173 >/dev/null 2>&1; do sleep 3; done'
          echo "✅ 前端服务已启动"

      - name: 验证服务状态
        run: |
          echo "🔍 验证服务状态..."

          # 检查后端服务
          if curl -f http://localhost:3000 >/dev/null 2>&1; then
            echo "✅ 后端服务响应正常"
          else
            echo "❌ 后端服务无响应，检查日志..."
            if [ -f /tmp/backend.pid ]; then
              ps -p $(cat /tmp/backend.pid) || echo "后端进程不存在"
            fi
            exit 1
          fi

          # 检查前端服务
          if curl -f http://localhost:5173 >/dev/null 2>&1; then
            echo "✅ 前端服务响应正常"
          else
            echo "❌ 前端服务无响应，检查日志..."
            if [ -f /tmp/frontend.pid ]; then
              ps -p $(cat /tmp/frontend.pid) || echo "前端进程不存在"
            fi
            exit 1
          fi

      - name: 运行Playwright测试
        run: npx playwright test
        env:
          CI: true
          NODE_ENV: test

      # 停止服务
      - name: 停止应用服务
        if: always()
        run: |
          echo "🛑 停止应用服务..."

          # 停止前端服务
          if [ -f /tmp/frontend.pid ]; then
            pid=$(cat /tmp/frontend.pid)
            kill $pid 2>/dev/null || true
            sleep 2
            kill -9 $pid 2>/dev/null || true
            rm -f /tmp/frontend.pid
            echo "✅ 前端服务已停止"
          fi

          # 停止后端服务
          if [ -f /tmp/backend.pid ]; then
            pid=$(cat /tmp/backend.pid)
            kill $pid 2>/dev/null || true
            sleep 2
            kill -9 $pid 2>/dev/null || true
            rm -f /tmp/backend.pid
            echo "✅ 后端服务已停止"
          fi

          # 强制清理端口
          lsof -ti:5173 | xargs kill -9 2>/dev/null || true
          lsof -ti:3000 | xargs kill -9 2>/dev/null || true

          echo "🧹 服务清理完成"

      # 上传测试报告
      - name: 上传Playwright报告
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report-${{ github.run_id }}
          path: |
            playwright-report/
            test-results/
          retention-days: 7

      # 上传测试结果摘要
      - name: 发布测试结果摘要
        if: always()
        run: |
          echo "## 🎭 Playwright 测试结果" >> $GITHUB_STEP_SUMMARY

          if [ -f "playwright-report/index.html" ]; then
            echo "- ✅ 测试报告已生成" >> $GITHUB_STEP_SUMMARY
            echo "- 📊 查看详细报告请下载 playwright-report 工件" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ⚠️ 未找到测试报告" >> $GITHUB_STEP_SUMMARY
          fi

          echo "- 🔗 提交: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "- 📅 测试时间: $(date)" >> $GITHUB_STEP_SUMMARY
          echo "- 🌐 测试环境: Ubuntu Latest + Node.js ${{ env.NODE_VERSION }}" >> $GITHUB_STEP_SUMMARY
